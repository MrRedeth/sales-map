services:
  web:
    build: .
    ports:
      - "8080:3000"
    environment:
      - DATA_FILE=/data/salesreps.json
    volumes:
      - salesreps-data:/data
    restart: unless-stopped

volumes:
  salesreps-data:
